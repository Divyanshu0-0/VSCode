#include <stdio.h> // Preprocessor directive

int main(){
    int num1 ,num2, sum;
    printf("enter the number");
    scanf("%d%d", &num1, &num2);
    sum=num1+num2;
    printf("the sum is: %d\n",sum);
    return 0;
}